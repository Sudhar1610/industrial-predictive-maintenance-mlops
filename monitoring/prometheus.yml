# Prometheus scrape config: pulls /metrics from the inference API.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "pdm_api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
