# Auto-provisions Prometheus as a Grafana datasource on container start,
# so `make up` gives a working dashboard with zero manual clicking.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
